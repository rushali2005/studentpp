specVersion: "v1"
serviceId: "studentpp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studentpp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
